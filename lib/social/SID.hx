package social;

/**
 * ID Пользователя социальной сети.
 * 
 * Представлен в виде строки, так как в разных соц. сетях uid
 * пользователей могут сильно отличаться от `Int`.
 */
typedef SID = String;