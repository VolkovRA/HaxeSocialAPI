package social.network;

/**
 * Поддерживаемый функционал и его параметры.
 * 
 * Этот объект содержит описание поддерживаемого функционала
 * и его различные параметры. Значения этих параметров могут
 * отличаться от сети к сети и реализации интерфейса.
 * 
 * Вы не должны изменять эти значения, они служат для
 * дополнительной информации и возможности более тесной
 * интеграции и оптимизации.
 */
typedef Support =
{
    >BaseSupport,
    >InviteFriendsSupport,
}

/**
 * Базовые параметры.
 */
typedef BaseSupport =
{
    /**
     * Ограничение на количество одновременно запрашиваемых данных 
     * пользователей в одном запросе к социальной сети.
     * 
     * Используется при вызове метода: `getUsers()`. Библиотека SocialNetwork
     * автоматически дробит большие запросы на несколько, если в одном из них
     * количество запрошенных пользователей превысит это значение.
     * 
     * Значение `0` - ограничение отсутствует.
     * 
     * | Социальная сеть | Значение |
     * |:-|:-|
     * |ВКонтакте|`1000`|
     * |Одноклассники|`0`|
     * |Facebook|`0`|
     * 
     * Не может быть `null`
     */
    var getUsersMax:Int;
}

/**
 * Параметры приглашения друзей.
 */
typedef InviteFriendsSupport =
{
    /**
     * Флаг доступности функционала.  
     * **Приглашение в игру**: Базовая поддержка метода.
     * - Если `true`, данная соц. сеть позволяет приглашать друзей в приложение.
     * - Если `false`, вызов этого метода генерирует исключение.
     * 
     * | Социальная сеть | Значение |
     * |:-|:-|
     * |ВКонтакте|`true`|
     * |Одноклассники|`true`|
     * |Facebook|`true`|
     * 
     * Не может быть `null`
     */
    var inviteFriends:Bool;

    /**
     * Флаг доступности функционала.  
     * **Приглашение в игру**: Поддержка передачи списка пользователей.  
     * - Если `true`, данная соц. сеть позволяет указать список приглашаемых
     *   в игру пользователей.
     * - Если `false`, передача списка пользователей не имеет эффекта в этой
     *   социальной сети.
     * 
     * | Социальная сеть | Значение |
     * |:-|:-|
     * |ВКонтакте|`false`|
     * |Одноклассники|`true`|
     * |Facebook|`true`|
     * 
     * Не может быть `null`
     */
    var inviteFriendsUsers:Bool;

    /**
     * Флаг доступности функционала.  
     * **Приглашение в игру**: Поддержка передачи сообщения.  
     * - Если `true`, данная соц. сеть позволяет указать произвольное сообщение
     *   вместе с приглашением друзей в игру.
     * - Если `false`, передача сообщения не имеет эффекта в этой социальной сети.
     * 
     * | Социальная сеть | Значение |
     * |:-|:-|
     * |ВКонтакте|`false`|
     * |Одноклассники|`true`|
     * |Facebook|`true`|
     * 
     * Не может быть `null`
     */
    var inviteFriendsMessage:Bool;

    /**
     * Флаг доступности функционала.  
     * **Приглашение в игру**: Поддержка получения результата приглашённых пользователей.
     * - Если `true`, данная соц. сеть позволяет получить список друзей, которым игрок
     *   отправил приглашение в игру.
     * - Если `false`, социальная сеть не позволяет получить список приглашённых.
     * 
     * | Социальная сеть | Значение |
     * |:-|:-|
     * |ВКонтакте|`false`|
     * |Одноклассники|`false`|
     * |Facebook|`false`|
     * 
     * Не может быть `null`
     */
    var inviteFriendsResult:Bool;
}